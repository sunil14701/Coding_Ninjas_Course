#include<iostream>
using namespace std;

int main(){
    // conditionals

    // if else 
    // int a, b;
    // cout << "Enter two numbers :";
    // cin >> a >> b;

    // // variations of if-else
    // if(a == b) cout << "two numbers are equal"<< endl;
    // else cout << "not equal" << endl;

    // if(a == b) cout << "yes they are equal" << endl;

    // if(a == b) cout << " equal"<< endl;
    // else if(a < b) cout << "a is smaller" << endl;
    // else cout << "b is greater" << endl;

    // number is even or odd 
    int num;
    cin >> num;
    if(num%2==0) cout << "Even" << endl;
    else cout << "Odd" << endl; 

    return 0;
}